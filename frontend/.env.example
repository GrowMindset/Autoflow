# Local development: Vite proxy target (/api -> VITE_API_URL).
VITE_API_URL=http://localhost:8000

# For backend-served frontend builds (single-domain/ngrok), keep VITE_API_URL
# empty/unset in production so API requests use same origin.
